一个运行在IOS上(Demo中有真机版本和模拟器版本的库)的一个MP4编辑的Demo

Demo实现如下功能：

加载一段本地MP4视频，可以是自己录制所得

记载完MP4文件后
(1) 可以选中某一段，为其添加动作，包括：裁剪，加滤镜，涂鸦(未实现)

(2) (如果不是裁剪Action，则可以)选中另一段，进行其他的Action添加。

(3) 把不同段的视频分别处理，拼接成最终输出，写回到MP4文件。

Demo中的FFMpeg的库的部分修改代码以后提供。